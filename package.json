{
  "name": "kubadu-admin",
  "version": "1.0.0",
  "description": "Kubadu Admin Panel",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 30000 test/*.selenium.test.js",
    "test:watch": "mocha --watch --timeout 30000 test/*.selenium.test.js"
  },
  "dependencies": {
    "selenium-webdriver": "^4.16.0",
    "chromedriver": "^131.0.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.10"
  }
}
